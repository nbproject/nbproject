require(['requirescript'], function() {
  require(['handlebars'], function(handlebars){
    require(['init_properties_ensemble_users'], function(init_properties_ensemble_users){
      require(['launch']);
    });
  });
});
