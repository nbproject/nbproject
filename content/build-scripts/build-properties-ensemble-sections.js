require(['requirescript'], function() {
  require(['handlebars'], function(handlebars){
    require(['init_properties_ensemble_sections'], function(init_properties__ensemble_sections){
      require(['launch']);
    });
  });
});
