require(['requirescript'], function() {
  require(['handlebars'], function(handlebars){
    require(['init_properties_ensemble'], function(init_properties__ensemble){
      require(['launch']);
    });
  });
});
